[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "napari-wsegmenter-worker"
version = "0.1.1"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
py-modules = ["napari_wsegmenter_worker"]
